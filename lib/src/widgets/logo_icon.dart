import 'package:flutter/material.dart';

class LogoIcon extends StatelessWidget {
  const LogoIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/img/general/logo_icon.png');
  }
}

class LogoFull extends StatelessWidget {
  const LogoFull({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/img/general/logo_full.png');
  }
}

class LogoFilled extends StatelessWidget {
  const LogoFilled({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/img/general/logo_filled.png');
  }
}
